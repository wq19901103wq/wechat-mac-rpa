# Experiments

This directory contains manual experiments and one-off validation scripts. They are not part of the bot's production startup path.

Older experiments are kept under `legacy/` for reference. Review their dependencies and environment variables before running them.
