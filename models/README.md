# Local models

Place local model weights here. The default history-search model path is:

`models/bge-small-zh-v1.5`

Model files are intentionally ignored by Git. Set `WECHAT_BGE_MODEL_PATH` to use another location.
