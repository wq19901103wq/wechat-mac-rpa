"""群聊 wiki 生成/更新的 prompt 模板，集中管理避免多处复制。"""


# 运行时增量更新用（更严格，含身份约束和结构要求）
RUNTIME_UPDATE_GROUP_PROMPT = """请根据以下对话记录，更新群聊 {chat_name} 的 wiki。

【已确认身份信息（必须遵守，禁止 contradict）】
{identity_context}

【更新规则】
1. wiki 是**编译后的摘要**，不是对话流水账——这是最重要的原则：
   - 严禁把原文对话（每条发言 + Bot 回复全文）逐条搬进 wiki
   - "近期话题 & 动态"只记**事件摘要**：每人每天最多 1-2 条，一句话概括发生了什么（如"2026-06-20 讨论AI能力，白姐认为ai干不好"）
   - 不要记录每条发言的原文、不要记录 Bot 的回复原文
2. 身份/关系信息（群成员画像、群规则文化）增量保留，新信息覆盖旧信息，冲突标 `[待验证]`
3. 超过 7 天的"近期动态"**必须删除**或并入历史，保持滚动窗口精简
4. 只修改/新增变化的部分，保留未变动的内容
5. 标注日期：时间敏感的信息必须带日期（格式：YYYY-MM-DD），日期必须严格来自对话记录开头的时间戳。禁止编造、推测、推断任何日期
6. 时间戳缺失：无法确定日期时不标注或用 [待验证] 标记
7. 冲突处理：新信息覆盖旧信息
8. 重点记录：
   - 群成员关系、身份、职业变化
   - 群内热点话题、事件、约定（摘要，非原文）
   - 群内文化、梗、常用语
   - 群规则、禁忌、注意事项
9. 别名发现：在"群成员画像"中，如果某个成员有多个称呼，请一并记录。只记录该成员本人的称呼，严禁把其他成员的名字误记到此成员下。格式：`成员主名（别名1/别名2）`
10. 多账号标注：如果对话来源包含不同账号标记，标注所属账号
11. 不确定的信息用 [待验证] 标记
12. 控制长度：群聊 wiki 不超过 10000 字（代码层有兜底截断，但仍请主动精简）。群成员画像和群规则文化是核心，优先保留完整。
13. 保持 Markdown 格式
14. **结构要求（必须遵守）**：wiki 必须包含以下 section，缺哪个补哪个：
    - `## 群基本信息`：群名、创建时间、群成员列表（一行列出所有成员主名+别名）
    - `## 群成员画像`：**每个活跃成员单独一段**，记录其身份/职业/性格特点/典型发言风格。这是核心 section，至少覆盖 10 个活跃成员，每人 1-3 句。严禁省略此 section 或把成员信息只塞进群基本信息
    - `## 群规则与文化`：群内梗、文化、常用语
    - `## 近期话题 & 动态`：滚动窗口，每人每天最多 1-2 条摘要

【现有 wiki】
{current_wiki}

【新对话】
群聊：{chat_name}
时间：{current_time}

对话内容：
{conversation}

【输出】
直接输出更新后的完整 wiki markdown，不要加代码块标记。"""


# 批量/首次生成用（更宽松）
BATCH_UPDATE_GROUP_PROMPT = """请根据以下对话记录，生成/更新群聊 wiki。

【现有 wiki】
{current_wiki}

【新对话】
群聊：{chat_name}
时间：{current_time}

对话内容：
{conversation}

【更新规则】
1. 只修改/新增变化的部分，保留未变动的内容
2. 标注日期：时间敏感的信息必须带日期（格式：YYYY-MM-DD）
3. 过期处理：超过 7 天的"近期动态"移到历史记录或删除
4. 冲突处理：新信息覆盖旧信息
5. 重点记录：
   - 群成员关系、身份、职业变化
   - 群内热点话题、事件、约定
   - 群内文化、梗、常用语
   - 群规则、禁忌、注意事项
6. 多账号标注：如果对话来源包含不同账号标记（如 [work]、[personal]），在 wiki 中标注该信息所属账号
7. 不确定的信息用 [待验证] 标记
8. **区分陈述和疑问/玩笑/反问（严格）**：以下句子严禁当作事实提取：
   - 以"吗"、"呢"、"?"、"？"结尾的句子
   - 反问句（如"不是你说我...的吗"）
   - 开玩笑/夸张表述（如"富比有多少资产"）
   - 用户考问Bot的问题（如"王乔元是谁"）
   这些属于交互行为，不是事实，严禁写入 wiki。
9. 控制长度：群聊 wiki 不超过 10000 字（代码层有兜底截断，但仍请主动精简）
10. 保持 Markdown 格式

【输出】
直接输出更新后的完整 wiki markdown，不要加代码块标记。"""


# 运行时默认模板
RUNTIME_DEFAULT_GROUP_WIKI = """# {group_name}

## 群基本信息
（暂无）

## 群成员画像
（暂无）

## 近期话题 & 动态
（暂无）

## 群内规则 & 文化
（暂无）
"""


# 批量默认模板
BATCH_DEFAULT_GROUP_WIKI = """# {group_name}

## 群基本信息
（暂无）

## 群成员画像
（暂无）

## 近期话题 & 动态
（暂无）

## 群内规则 & 文化
（暂无）
"""
