# 核心运行依赖
openai>=2.45.0
defusedxml>=0.7.1
numpy==1.24.3
sqlalchemy>=2.0.51,<3.0.0
scikit-learn==1.2.1
scipy>=1.10.0,<1.16
Pillow>=12.3.0,<13.0.0
requests>=2.34.2
httpx>=0.28.1
beautifulsoup4>=4.15.0

# 微信自动化与 OCR
pyautogui>=0.9.54
pytesseract>=0.3.13
pyobjc-framework-Vision>=12.2.1

# 智能家居工具
tinytuya>=1.19.0
paho-mqtt>=2.1.0

# Web 服务
fastapi>=0.137.1
uvicorn>=0.52.1

# 历史原文检索（search_history 工具，可选）
# 复用 digital-twin 的 BGE 消息级索引（77万条历史微信消息原文）。
# 仅当 tokenizers+onnxruntime 可 import 且索引文件存在时，generator 才注册
# search_history 工具；否则跳过，bot 行为零变化。
# 索引/模型路径见 src/memory/history_search.py 顶部默认值与环境变量。
tokenizers>=0.23.1
onnxruntime>=1.16.0
